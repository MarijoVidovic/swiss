import { Route } from '@angular/router';
import { OverviewViewComponent } from './features/components/overview-view/overview-view.component';

export const appRoutes: Route[] = [
    { path: '', component: OverviewViewComponent },
  ];
